import React from "react"

import DeviceItem from "../device-item"

const DevicePage = ({ id }) => {
    return (
        <div>
            <DeviceItem id={id} />
        </div>
    )
}

export default DevicePage
